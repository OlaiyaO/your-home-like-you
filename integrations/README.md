# Integrations

Optional operational integrations live here and are not part of the storefront runtime.

- `google-forms/` provides the staff-only, append-only catalogue Forms workflow that rebuilds the Google Sheet `Products` tab used for validated CSV snapshot publishing.

Integrations must not contain storefront secrets, customer/order data, or direct checkout and payment behaviour.
